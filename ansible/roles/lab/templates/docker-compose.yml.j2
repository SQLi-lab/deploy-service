version: '3.8'

services:
  lab_{{ UUID }}:
    image: nginx:latest
    container_name: {{ UUID }}
    restart: always
    networks:
      - default

networks:
  default:
    external: true
    name: bridge

